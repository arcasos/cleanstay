# 클린콜(CLEANCALL) — STR 청소 디스패치 플랫폼 · 프로젝트 시드 문서

**작성일**: 2026-07-19 (v5)
**용도**: 신규 Claude 프로젝트 생성 시 Project Knowledge로 첨부. 새 세션 시작 시 가장 먼저 읽는 레퍼런스.
**출처**: ARCASOS 프로젝트에서의 전략·정산·결제 구조 논의 핸드오프 (ARCASOS = 클린콜의 수요 고객 #1).
**상태**: 컨셉·정산·결제·공급자 온보딩·배차 모델·디자인 시스템 확정. **API 스펙 v0.3.1 확정, 코어 EF 4종 구현 완료(미배포).** 도메인·일부 정책 미정(§13).

> ### 📝 v5 변경 이력 (2026-07-19) — ARCASOS 스펙 협의 3라운드 + 구현 반영
>
> **⚠️ 이 갱신이 늦었다.** v4 이후 OpenAPI가 v0.1→v0.3.1로 세 번 바뀌는 동안 이 문서를 고치지 않아,
> "SSOT"라고 선언된 문서가 실제로는 레포에서 가장 오래된 문서가 되어 있었다. 새 세션이 시드를 읽고
> 틀린 전제로 작업하는 상태였다. **앞으로 결정이 날 때마다 이 문서에 즉시 반영한다.**
>
> **OpenAPI v0.3.1과 어긋나 있던 것 정정**
> - §5 `trigger_type` 에 `rework` 추가 (v0.2에서 추가됨)
> - §5 `GET /v1/orders/{id}/claim` → **`/claims`**(복수) + `POST` 추가 (v0.3에서 변경)
> - §5 webhook `order.billing_verified` → **`order.dispatch_unblocked`** (v0.3에서 개명)
>
> **§13 해소**: #3(긴급 프리미엄=0) · #4(자동확정 공식) · #10(공급자 승인=운영자 최종)
> **§13 신규**: 무보상 시한 24h · 단가 체계 · `webhook_max_age` · `preferred_provider`
>
> **시드에 없던 개념 반영**
> - **출입 정보 2층**(§4 객체 · §5 API · §9 보안) — 도어락은 게스트마다 바뀌므로 매물이 아니라 발주 속성
> - **`scheduled_at` / `arrival_at`**(§4 생명주기) — "공급자가 청소 예정 시각을 정한다"는 단계가 없었다
> - **커버리지 / `pending_coverage`**(§5 · §14 B) — 미지원 지역 매물 처리가 정의돼 있지 않았다
> - **env 분리(live/test)**(§5 · §11) — 스키마 전반. 복합 FK로 교차 참조 차단
> - **백업 공급자 사전 지정**(§6) — Turno 벤치마킹
> - **§1 경쟁 지형 정정** — "아무도 안 한다"는 과장이었다
> - **§7 호스트 직접 경로** — 에어비앤비 iCal 검증됨, 국내 OTA 미확인
> - **§15 신설** — 숙박 기간 확장 시 재검토 목록
>
> ⚠️ §15로 append한 이유: §12는 이미 "범위 밖"이고, 이 문서는 **섹션 번호 안정성**을 명시적 규칙으로
> 두고 있다(v4 이력 참조). 번호를 밀면 애드덤·외부 참조가 깨진다.

> ### 📝 v4 변경 이력 (2026-07-18) — 애드덤 2건 병합
> **병합 원본**: 「공급자 온보딩·배차 정책 애드덤」 + 「디자인 시스템 전환 애드덤」. 두 애드덤은 이 문서에 흡수되었으며, 이후 SSOT는 이 문서입니다.
> - **§4 공급자 유형 분기**: `business`(업체) / `individual`(개인·이모님). `provider_service_areas`·`provider_events` 객체 추가.
> - **§6 배차 모델 확정**: 순수 브로드캐스트 → **예약형 랭크드 오퍼 웨이브**(Wave 1~4). **강제 배정 금지, 오퍼→수락 유지**(독립계약자 방패). §13 #2 해소.
> - **§14 신설**: 공급자 온보딩(사업자 진위확인/본인인증) · 활동지역(법정동코드 접두) · 배차 정지 상태머신.
> - **§5 API 표면**: 공급자 등록/검증/활동지역/정지 엔드포인트 추가.
> - **§10 디자인 전면 교체**: **Linear Dark 폐기 → 클린콜 자체 시스템(라이트 + 파랑, 시맨틱 토큰)**. 아르카소스는 Linear Dark 유지, `design-agent` 스킬은 **클린콜에 적용하지 않음**.
> - **§13**: #2 해소(오퍼 웨이브), 신규 오픈 항목 "다크모드 적용 시점" 추가.
> - ⚠️ **섹션 번호는 v3와 동일하게 유지**(§10=기술·디자인, §13=오픈 결정). 애드덤·외부 문서의 참조가 깨지지 않도록 신규 내용은 §14로 append.

> ### 📝 v3 변경 이력 (2026-07-18)
> - **§9 결제 연결 3-tier 확정**: Hosted Link / OAuth 리다이렉트 / Embed SDK. 겉 UX는 테넌트별로 달라도, **빌링키 볼팅 코어는 단일**(카드/빌링키 = 클린콜·PG만, 테넌트 = 참조 토큰만). 아르카 = Tier 2(OAuth) 레퍼런스.
> - **자동 프로비저닝 확정**: 호스트는 "클린콜 회원가입" 화면을 안 봄. tenant-scoped 계정 자동 생성. **단, 결제·정기결제·개인정보 동의는 호스트 본인이 결제창에서 직접**(법적 생략 불가).
> - **§4/§5/§8 발주 원장·보상 추적 추가**: 상호 참조 ID(클린콜 `order_id` ↔ 아르카 `booking_id`), append-only 이벤트 원장, 귀책 필드, 완료 증빙, `claim` 테이블 골격. **클린콜 = SoR / 아르카 = webhook 미러 + reconciliation**.
> - **§5/§9/§11 결제 타이밍 확정**: **발주(체크인) = 빌링키 유효성 검증(청구 X, 배차 게이트) / 완료 보고 후 = 실제 청구(재결제) → 공급자 지급.** 취소·중도퇴실 = 청구 없음(환불 로직 불필요).
> - **발주/청소 시점 명확화**: 발주 생성 = 체크인 시점 / 청소 시작 = 체크아웃 시점.

> ### 📝 v2 변경 이력 (2026-07-18)
> - 정산 구조: 아르카 대납·차감 폐기 → **호스트 직결제(빌링키)**. 아르카는 청소비 흐름에서 배제(발주 이벤트만).
> - 세금계산서: 클린콜 → 호스트 발행. 전자지급결제대행·자금보유 규제 회피.
> - §6 중도퇴실 긴급배차, §5 `trigger_type`/`priority` 추가. 프로젝트명 클린콜(CLEANCALL) 확정.

> ⚠️ 이 문서는 개념·정산·결제·디자인 설계 산출물입니다. 코드·DB·API 스키마는 아직 미구현이며, "무엇을·왜·어떤 순서로 만드는가"의 합의된 방향입니다. 구현 시 이 문서를 SSOT로 삼되, 결정이 바뀌면 반드시 이 문서에 반영할 것.

---

## 0. Project Instructions (새 세션용 — 아래를 프로젝트 지침에 붙여넣기)

> "당신은 STR(단기임대) 체크아웃 청소를 위한 **API-first 배차 플랫폼 '클린콜'** 개발을 돕는 시니어 개발자입니다. 이 제품은 UI 앱이 아니라 **인프라 제품**(Stripe/Twilio 계열)입니다 — 공개 API + webhook이 본체입니다. 수요측(숙박 플랫폼)과 공급측(청소업체)을 잇는 양면 미들웨어이며, 배차 모델은 **예약형 랭크드 오퍼 웨이브**('청소업의 배민', 단 강제 배정 없이 오퍼→수락)입니다. 첫 고객은 ARCASOS(단기임대 플랫폼, 수요 #1)와 창업자 직영 청소업(공급 #1)입니다. **과금은 호스트 직결제(빌링키)** — 클린콜이 호스트에게 직접 청구·세금계산서 발행하고, 아르카소스는 청소비 흐름에 관여하지 않고 발주 이벤트만 API로 전달합니다. **결제 연결은 3-tier**(Hosted Link/OAuth/Embed)이되 빌링키 볼팅 코어는 단일이며, **결제 타이밍은 발주 시 유효성 검증 + 완료 후 청구**입니다. 공급자는 **업체/개인 두 유형**으로 온보딩하며(사업자 진위확인 / 본인인증), 활동지역은 **법정동코드 접두 계층**으로 저장·매칭합니다. 클린콜은 발주 원장의 SoR(System of Record)이고 테넌트는 webhook 미러입니다. 스택은 React/TypeScript + Supabase + Deno Edge Functions. **UI는 클린콜 자체 디자인 시스템(라이트 + 파랑, 시맨틱 토큰 전용)을 쓰며 아르카소스의 Linear Dark와 `design-agent` 스킬은 절대 적용하지 않습니다(§10).** ARCASOS 검증 패턴(알림톡/원천징수/MCP)을 포팅하되 독립 레포·Supabase·도메인으로 분리 운영합니다. 코드 수정 시 항상 타입체크(`npx tsc --noEmit`) 후 파일 제공."

---

## 1. 제품 정의

**한 줄**: STR 체크아웃(턴오버) 청소를 자동 발주·배차하는 API 레이어.

**왜 지금 이 자리인가**: 국내 청소 플랫폼(청소연구소·미소·클린벨 등) **누구도 공개 API를 제공하지 않는다**(2026-07 확인). 숙박 운영자가 "체크아웃 → 청소 자동 발주"를 하고 싶어도 붙일 API가 시장에 없다. 그 API가 되는 것이 클린콜의 자리다.

### 경쟁 지형 (v5 정정)

> ⚠️ v4까지 "아무도 안 한다"로 읽히게 써 있었다. **과장이다.** 정확히는 "국내에 **자동 발주·배차**가 없다"이지, 숙박 청소 사업자가 없다는 뜻이 아니다.

| 구분 | 사업자 | 방식 |
|---|---|---|
| **국내 숙박청소 전문** | 클린헬퍼 · 유즈미 · 월드클리너스 | **전부 수동 접수 + 현장 실사.** 전화·카톡으로 받고 사람이 배정. 공개 API 없음 |
| **국내 일반청소 플랫폼** | 청소연구소 · 미소 · 클린벨 | 소비자 대상 앱. STR 턴오버 특화 아님. 공개 API 없음 |
| **해외** | **Turno**(2016, 舊 TurnoverBnB. 청소부 약 55,000명) | 호스트 SaaS + **청소부 입찰 마켓플레이스**. iCal 연동으로 예약 읽어 자동 스케줄 |

**시사점 세 가지**
1. **모델 자체는 검증됐다** — Turno가 10년째 돌고 있다. 미검증 가설이 아니라 국내 미이식 모델이다.
2. **국내 공백은 "자동화"에 있다** — 수요는 이미 수동으로 처리되고 있다. 클린콜의 차별점은 시장 창출이 아니라 **수동 → API 전환**이다.
3. **입찰 vs 랭크드 오퍼** — Turno는 청소부가 가격을 제시하는 입찰이다. 우리는 §6의 랭크드 오퍼를 택했다. 입찰 도입 여부는 §13 신규 항목으로 열어둔다.

**핵심 통찰**: 실시간 소비자 청소(입주·정기)가 아니라 **STR 턴오버 청소**로 좁힌다. 턴오버는 (1) 같은 매물 반복 → 규격 표준화, (2) 체크아웃~다음 체크인이라는 명확한 시간 윈도우, (3) "다음 게스트 맞을 준비 완료" 단일 완료 기준 → 배차·단가·품질을 딱 떨어지게 잡을 수 있다.

---

## 2. 핵심 원칙 (절대 준수)

### 원칙 A — API-first (인프라 제품)
발주의 주력 경로는 사람이 클릭하는 UI가 아니라 **숙박 플랫폼의 이벤트가 API로 자동 생성**하는 것. 관리 콘솔·공급자 앱은 보조. 설계·문서·우선순위 모두 API + webhook 우선.

### 원칙 B — 프론트도어 (Front-door)
ARCASOS도 **내부 백도어로 붙이지 않는다.** 미래 외부 고객(야놀자·독립 STR 운영자)이 쓸 그 공개 API와 **동일한 문**으로 들어온다. 발주만 API로 넘기고, **과금은 클린콜↔호스트 직거래**(§9). 겉 연결 UX는 테넌트별로 달라도(§9 3-tier) **빌링키 볼팅 코어는 단일** — 카드/빌링키는 클린콜·PG에만, 테넌트는 토큰만.

### 원칙 C — 양면 부트스트랩 (Bootstrap)
콜드스타트 회피를 위해 **양쪽 첫 노드를 창업자가 직접 쥔다**: 수요 #1 = ARCASOS, 공급 #1 = 창업자 직영 청소업. MVP에 외부 참여자 0명으로 루프 전체를 실제로 돌린다.
- ⚠️ **내 청소업 = 부트스트랩 시드이지 최종 공급모델이 아님.** 플랫폼 가치는 결국 다수 업체 집계.
- ⚠️ **배차 공정성은 미래 신뢰 문제**(아마존 vs 셀러 구도). 배차 로직은 처음부터 "나중에 공정해질 수 있게" 설계 → v4에서 **랭킹 함수의 공정성 가중치**로 구체화(§6). 직영팀도 동일 규칙으로 콜 수신.

### 원칙 D — 지휘·감독 관계 금지 (v4 신설)
플랫폼은 **오퍼를 보내고 공급자가 수락**한다. **강제 배정(auto-assign)·업무 명령 금지.** 이를 어기면 §9의 독립계약자 구조(3.3% 원천징수·직접고용 회피)가 무너진다. 배차 UX·API·문구 전부 "콜 수신 → 수락" 형태를 유지할 것.

---

## 3. 정체성 — 예약형 콜 브로드캐스트 ("청소업의 배민")

| 배민 | 클린콜 |
|---|---|
| 음식점(주문 발생) | 숙박 플랫폼(체크아웃 → 청소 발주) |
| 배민 라이더 앱 | 청소업체/이모님 파트너 앱 |
| 콜 뜸 → 라이더 수락 | 청소콜 뜸 → 업체 수락 |
| 배달 완료 사진 | 청소 완료 사진 인증 |
| 정산 | 호스트 직결제 + 공급자 지급(3.3% 원천징수) |

**배민과의 결정적 차이 = 유리한 점**: 청소는 **체크아웃이 며칠 전 확정** → **예약형 콜**. 리드타임 윈도우 안에서 여러 번 재배차 여유. 초단위 매칭 엔진 불필요.

**배민이 푸는 어려운 문제 = 클린콜도 풀어야 함**: (1) 미배차 → 에스컬레이션·백업 배차, (2) 콜 분배 로직(→ v4에서 오퍼 웨이브로 확정, §6), (3) 노쇼·펑크(다음 체크인 펑크 = 치명적) → 백업 배차+마감 알림, (4) 품질/분쟁 → 완료사진+체크리스트+호스트 확인 + **공급자 정지 상태머신**(§14).

> **용어**: 공급자에게 노출되는 단어는 계속 **"콜"**. 밑단 로직이 랭크드 오퍼라는 건 내부 구현어이며 UI 문구로 쓰지 않는다.

---

## 4. 객체 모델 + 발주 생명주기

### 객체
| 객체 | 정의 |
|---|---|
| **테넌트(Tenant)** | 발주하는 숙박 플랫폼. API 키 발급 단위. (ARCASOS #1) |
| **호스트(Host)** | 청소값을 직접 결제하는 매물 소유자. 빌링키 등록 단위. tenant-scoped 자동 프로비저닝(§9). |
| **공급자(Provider)** | 청소를 수행하는 주체. **`type` = `business`(업체) \| `individual`(개인·이모님)** 로 분기(§14 A). 파트너 앱 사용. |
| **활동지역(ProviderServiceArea)** | 공급자의 콜 수신 범위. `{region_code, level}` 행 집합, 법정동코드 접두(§14 B). |
| **공급자 이벤트(ProviderEvent)** | append-only. 온보딩·경고·정지·해제 이력 공용 원장(§14 A/D). |
| **발주(Order)** | 하나의 턴오버 청소 건. 아래 원장 필드 포함. |
| **클레임(Claim)** | 발주에 연관된 보상/분쟁 건(§8). 골격만 지금, 정책은 나중. |
| **매물 출입정보(PropertyAccessInfo)** | 매물 단위 **고정** 출입 안내(공동현관·엘리베이터·주차). 암호화 저장(v5). |
| **발주 출입정보(OrderAccessInfo)** | 그 발주 건에만 적용되는 출입 정보(**도어락 비밀번호**). 암호화 저장(v5). |
| **출입 토큰(AccessUpdateToken)** | 호스트가 로그인 없이 출입정보를 넣는 1회성 링크. 평문은 URL에만, DB에는 sha256(v5). |

### 출입 정보 — 2층 분리 (v5 신설)

**왜 2층인가**: 도어락 비밀번호는 **게스트마다 바뀐다.** 매물 속성으로 두면 발주마다 덮어써야 하고,
과거 발주의 출입정보가 소급 변경되어 "공급자가 그때 무엇을 받았는가"를 재구성할 수 없다.
고정값과 가변값을 분리한다.

| 층 | 내용 | 갱신 주기 |
|---|---|---|
| **매물 단위** | 공동현관 번호, 엘리베이터, 주차, 분리수거 위치 | 거의 안 바뀜 |
| **발주 단위** | 도어락 비밀번호 | **게스트마다** |

**보안 규약 (절대)**
- **테넌트에게 값을 반환하지 않는다.** 등록 여부(`registered`/`not_registered`)와 `updated_at`만 노출한다.
  아르카는 도어락 비밀번호를 알 필요가 없고, 알면 그 자체가 사고 표면이다.
- **호스트가 직접 입력한다.** 테넌트가 대신 전송하면 개인정보 제3자 제공 문제가 된다.
- **공급자에게는 배차 확정 후에만** 노출한다(`address_detail` 동일).
- 링크 토큰의 평문은 URL에만 존재하고 DB에는 sha256만 남긴다. API 키와 같은 규율이다.
- `access_info_stale` 경고: 청소 임박(6시간 전)인데 발주 단위 출입정보가 미갱신이면 테넌트에 통지.
  **배차 보류가 아니라 경고다** — 배차는 진행되지만 공급자가 문 앞에서 못 들어갈 위험이 있다.

### 발주 원장 필드 (차후 보상 성립을 위해 지금 심을 것 — §8)
- **상호 참조 ID**: 클린콜 `order_id` ↔ 테넌트 `tenant_ref`(아르카 `booking_id`). **양쪽 저장.**
- **append-only 이벤트 이력**: 상태 전이·시각·행위자. 덮어쓰기 금지, 이벤트 누적.
- **금액 스냅샷**: 청구액·긴급 프리미엄·원천징수액.
- **귀책(fault)**: 실패·취소 귀책 주체(tenant/provider/host/guest). 보상 판단 근거.
- **완료 증빙**: 사진·체크리스트·타임스탬프. 클린콜이 원본, 테넌트는 참조.
- **오퍼 이력(v4 추가)**: 어느 공급자에게 몇 차 웨이브로 언제 오퍼했는지. "몰아주기 불가" 감사 로그(§6).

### 발주 생명주기
```
created            (체크인 시점에 발주 생성, trigger_type·priority 부여)
  → billing_verified (배차 게이트 통과 — 빌링키 유효 AND 출입정보 등록 §9)
    → broadcasting   (가용 공급자 풀에 오퍼 웨이브 개시 §6)
      → accepted     (공급자 수락 → 배차 확정, **scheduled_at 확정**)
        → in_progress (청소 시작, **arrival_at 기록**)
          → completed (완료사진 + 체크리스트 제출, completed_at)
            → confirmed (호스트 확인 또는 자동확정 — 공식은 §13 #4)
              → charged  (완료 후 빌링키 재결제 = 실제 청구)
                → paid_out (공급자에게 3.3% 원천징수 후 지급)

[예외 — 청구 없음]
  billing_verified 실패 → 배차 보류 + 호스트 "결제수단 확인" 알림
  broadcasting → (전 웨이브 미수락) → escalated → backup_dispatch
  accepted → (노쇼/취소) → reassigning → backup_dispatch
  중도퇴실(§6) → 기존 order cancelled(reason: early_checkout) + 긴급 신규 order
  * 취소·중도퇴실 건은 청구 자체가 없으므로 환불 로직 불필요
  * 각 상태 변화는 테넌트에게 webhook 통지
```

### 시각 필드 (v5 — 생명주기에 빠져 있던 것)

v4의 생명주기에는 **"공급자가 언제 청소하러 오는가"가 없었다.** 호스트 화면의 1번 질문인데 필드가 없었다.

| 필드 | 의미 | 확정 시점 |
|---|---|---|
| `checkin_at` / `checkout_at` | 게스트 예약 창. `checkout_at` = 청소 시작 가능 시각 | 발주 생성 |
| `next_checkin_at` | 다음 예약 체크인 = 마감 힌트. **선택** | 발주 생성 또는 나중에 PATCH |
| `deadline_at` | 서버 계산 마감 = `next_checkin_at ?? (checkout_at + cleaning_deadline_hours)` | 발주 생성 |
| **`scheduled_at`** | **공급자가 정한 청소 예정 시각.** 호스트 화면의 "언제 오는지" | `accepted` |
| **`arrival_at`** | 공급자 실제 도착 시각 | `in_progress` |
| `free_change_until` | 무보상 변경·취소 시한. 배차 전 `null`, 배차 후 `scheduled_at - 24h` | `accepted` |
| `auto_confirm_at` | 자동 확정 시각. 공식은 §13 #4 | `completed` |

**`free_change_until` 불리한 소급 금지**: 공급자 사유로 `scheduled_at`이 앞당겨져도 이 값은 줄지 않는다
(`max(기존, 새 값)`). 호스트가 아무것도 하지 않았는데 보상 대상 구간에 들어가는 일은 없어야 한다.
뒤로 미뤄지는 경우는 호스트에게 유리하므로 그대로 갱신한다.

**`sequence`**: 발주별 단조 증가. webhook 순서 판정의 근거이며 DB 트리거가 올린다.
테넌트에게 노출되는 필드가 바뀌면 오른다 — 원장에 행이 쌓이는데 `sequence`가 그대로면
수신 측이 "작거나 같으면 무시" 규칙으로 두 번째 이벤트를 버린다.

> 상태별 UI 색상 매핑은 §10 및 「디자인 시스템 전환 애드덤」 §C 표를 따른다. 상태 표기가 색으로 읽히는 것이 콘솔 UX의 핵심이다.

---

## 5. 공개 API 표면 (초안 — 구현 시 확정)

**수요측(테넌트용) REST**
| 메서드 | 엔드포인트 | 용도 |
|---|---|---|
| POST | `/v1/orders` | 발주 생성 (property, checkin_at, checkout_at, spec, trigger_type, priority, tenant_ref) |
| GET | `/v1/orders/{id}` | 발주 상태·이력 조회 |
| POST | `/v1/orders/{id}/cancel` | 발주 취소 |
| POST | `/v1/properties` | 매물 등록/매핑 (주소·평형·스펙·호스트·`region_code`) |
| GET | `/v1/properties/{id}` · PATCH | 매물 조회·수정 |
| PATCH | `/v1/orders/{id}` | 발주 수정(`checkout_at`·`next_checkin_at`·`spec`). `dry_run` 지원 |
| POST | `/v1/orders/{id}/early-checkout` | 중도퇴실 → 기존 취소 + 긴급 신규 |
| POST | `/v1/orders/{id}/messages` · GET | 공급자 중계 연락(연락처 비노출) |
| POST | `/v1/hosts/{id}/setup/link` | 호스트 설정 링크(결제수단·출입정보). 매물 일괄 처리 |
| **POST** | **`/v1/orders/{id}/claims`** · GET | 보상/분쟁 접수·조회 — **v0.3에서 복수형으로 변경** |
| GET | `/v1/coverage` · POST `/v1/coverage/check` | 서비스 지원 지역 조회·확인 |
| POST | `/v1/sandbox/orders/{id}/transition` | 상태 강제 전이 (**테스트 키 전용**) |

**공급측 / 운영 (v4 추가)**
| 메서드 | 엔드포인트 | 용도 |
|---|---|---|
| POST | `/v1/providers` | 공급자 가입 (`type`, 기본 정보) |
| POST | `/v1/providers/{id}/verify-business` | 사업자 진위확인 + 상태조회 (업체) |
| POST | `/v1/providers/{id}/verify-identity` | 본인인증 (개인) |
| PUT | `/v1/providers/{id}/service-areas` | 활동지역 `{region_code, level}[]` 등록·수정 |
| POST | `/v1/providers/{id}/suspend` | 배차 정지 (사유·기간 필수, 운영자 전용) |
| POST | `/v1/providers/{id}/reinstate` | 정지 해제 (운영자 전용) |
| GET | `/v1/providers/{id}/events` | provider_events 타임라인 조회 |
| GET | `/v1/offers` · POST `/v1/offers/{id}/accept` \| `/reject` | 공급자 앱: 콜 수신·수락·거절 |

**Order 필드(핵심)**
- `trigger_type`: `scheduled`(정상) | `early_checkout`(중도퇴실) | **`rework`(재청소 — v0.2 추가)**
- `priority`: `normal` | `urgent`
- `tenant_ref`: 테넌트측 예약 식별자(아르카 booking_id) — 상호 참조
- 시간 윈도우: `checkin_at` < `checkout_at`(청소 시작 하한) ~ `deadline_at`(마감)
- **멱등키 = `(tenant_id, tenant_ref, trigger_type, env)`** — `rework`는 `scheduled`와 키가 분리되므로
  같은 `tenant_ref`로도 별도 발주가 생성된다. 타임아웃 시 그냥 재호출하면 기존 발주가 반환된다.

**env 분리 (live / test) — v5 신설**

테스트가 운영 데이터를 건드리지 않아야 한다. API 키의 `env`가 그 요청의 데이터 공간을 결정한다.

- `ck_live_...` / `ck_test_...` 두 계열. `/sandbox/*`는 test 키 전용(live면 403).
- `orders`·`properties`·`hosts`·`billing_keys`·`tenant_api_keys`가 `env` 컬럼을 갖는다. **DEFAULT 없음** —
  누락 시 조용히 live로 떨어지는 대신 not-null 위반으로 즉시 실패한다.
- 자식 테이블(`order_events`·`claims`·`payments` 등)은 부모에서 유도 가능하므로 `env`를 두지 않는다.
  중복 저장은 드리프트를 만든다.
- **복합 FK로 교차 참조를 DB가 거부한다** — `orders.(property_id, env) → properties.(id, env)` 등.
  코드 필터가 유일한 방어선이면 언젠가 뚫린다(실제로 세 번 뚫렸다).

**커버리지 / `pending_coverage` — v5 신설**

§12가 "서울만"이라고만 했지 **미지원 지역 매물을 어떻게 할지가 정의돼 있지 않았다.**

| 상황 | 처리 |
|---|---|
| 서울 안, 공급자 확보된 동 | `status = active` → 발주 가능 |
| **서울 안, 공급자 없는 동** | `status = pending_coverage` → **등록은 받되 발주는 409**. 커버리지 확장 시 `property.activated` webhook |
| **서울 밖 (부산 등)** | **422 `region_unresolved`** — `pending_coverage`가 아니다 |

**"모르는 지역"과 "공급자 없는 동"은 다르다.** `pending_coverage`는 서울 안에서 곧 열릴 수 있는 동을
위한 상태다. 부산은 서비스 계획 자체가 없으므로 등록을 받으면 안 된다. 받아두면 영원히 열리지 않는
매물이 원장에 남고, 호스트는 기다린다.

지역 해석 우선순위: `region_code` > `lat`/`lng`(WGS84, 카카오 `coord2regioncode`) > `address`.
**주소 문자열 지오코딩은 범위 밖** — 좌표 없이 주소만 오면 422.

**Webhook (플랫폼 → 테넌트)** — 미러링·보상 대사용, 귀책·금액 스냅샷 포함
| 이벤트 | 시점 |
|---|---|
| **`order.dispatch_unblocked`** | 배차 게이트 통과(결제수단·출입정보 충족) — **v0.3에서 `order.billing_verified`를 개명** |
| `order.dispatched` | 공급자 수락·배차 확정 (`scheduled_at` 확정) |
| `order.rescheduled` | `scheduled_at` 변경. 상태는 그대로 |
| `order.access_info_stale` | 청소 6시간 전인데 출입정보 미갱신 (**경고**, 보류 아님) |
| `order.in_progress` | 청소 시작 |
| `order.completed` | 완료사진 제출 |
| `order.confirmed` | 호스트 확인 또는 자동확정 |
| `order.charged` | 완료 후 실제 청구 성공 |
| `order.cancelled` | 취소 (reason 포함) |
| `order.urgent_created` | 중도퇴실 등 긴급 신규 order |
| `order.failed` | 미배차/노쇼/결제실패 등 (귀책 + `stage` 포함) |
| `claim.updated` | 보상/분쟁 상태 변화 |

> `order.paid_out`은 **발송하지 않는다.** 공급자 지급은 테넌트의 관심사가 아니다.

**전송 규약 (v5 구현 확정)**
- 서명 `HMAC-SHA256(secret, "{timestamp}.{raw_body}")`, 헤더 `X-Cleancall-Signature` / `-Timestamp` / `-Delivery`.
  timestamp를 서명에 포함해야 재전송 공격을 막는다.
- payload에 `sequence` + `occurred_at`. **이미 처리한 `sequence`보다 작거나 같으면 무시**할 것.
- 재시도 1m→5m→15m→1h→6h→24h, 6회 후 `dead`. 5초 타임아웃. **4xx는 재시도 안 함**, 5xx·타임아웃은 재시도.
- 같은 `order_id`는 `sequence` 순서대로 나간다. 낮은 것이 미완료면 높은 것을 붙잡아 둔다.
- secret 로테이션 시 구 secret 24시간 동시 유효(수신 측 관용). **발신은 항상 현재 secret으로만.**
- **R6 — fire-and-forget 금지.** cron이 `next_retry_at`을 스캔하는 백스톱이 있어야 한다(§8).

**공급측(공급자 앱)**: 콜 수신 → 수락/거절 → 시작 → 완료(사진+체크리스트). 청소업체가 API를 안 주는 현실상 직접 구축(미소 파트너·클린벨 파트너 앱 대응물).

- 인증: 테넌트 = API 키, 공급자 = 세션 토큰. service_role EF는 **caller 신원 검증 필수**(SEC-04 패턴).
- CORS: `*` 금지, allowedOrigins 화이트리스트.

---

## 6. 배차 엔진 설계 (제품의 심장)

**확정 모델: 예약형 랭크드 오퍼 웨이브** (v3의 "순수 브로드캐스트"를 대체 — §13 #2 해소)

```
발주 생성(체크인, 빌링키 검증 통과) → 리드타임 = checkout_at ~ checkin_at
  → 가용 공급자 풀 산출
       필터: status ∈ {active, warned}
             AND 활동지역 접두 매칭(§14 B)
             AND 가용 시간대
  → Wave 1: 랭킹 상위 소수에게 우선 오퍼 (우선 수락창 T1분)
       랭킹 = 지역근접 · 평점 · 신뢰도(노쇼율↓·완료율↑) · 공정성 가중(최근 배차 적은 순)
  → Wave 2: 미수락 → 반경 확대 + 인센티브 상향
  → Wave 3: 전체 오픈 (순수 선착순)
  → Wave 4: 미배차 → 백업 배차(직영팀 보증) + 테넌트 알림
  → 배차 후 노쇼/취소 감지 → 재배차 루프(리드타임 잔여 활용)
  → 마감 임박(체크인 N시간 전) → 강제 알림·수동 개입
```

**왜 순수 브로드캐스트보다 나은가**
1. **공정성 튜닝 가능** — 원칙 C의 "나중에 공정해질 수 있게"가 랭킹 함수 하나로 해결(선착순은 UX 전체를 갈아야 함).
2. **리드타임 활용** — 예약형이라 초단위 경쟁 불필요, 웨이브 창 여유.
3. **품질 자기강화 + 스팸↓** — 잘하는 공급자 우선, 못 잡을 콜 난사 방지.
4. **감사 로그** — 오퍼 순서·사유 기록 = "몰아주기 불가 로그" 자동 확보(§4 오퍼 이력).

**법적 필수 조건 (원칙 D)** — **강제 배정 금지, "오퍼 → 공급자 수락" 유지.** 플랫폼이 일방 배정·명령하면 지휘·감독 관계로 비쳐 §9의 독립계약자·고용리스크 회피가 깨진다.

**MVP 경로**: 초기엔 Wave 1을 생략하고 "지역·평점 필터된 풀에 오픈 오퍼"로 시작 가능. 단 **풀 산출·랭킹 필드를 처음부터 심어** 두면 나중에 Wave만 얹으면 된다.

### 중도퇴실 긴급배차 (early_checkout)
```
중도퇴실 이벤트(호스트 확인 게이트 후 발동 — 완전 자동은 오배차 리스크)
  ├─ 기존 order 자동취소 (cancelled, reason: early_checkout)
  │    broadcasting(미수락) → 조용히 취소, 손실 없음
  │    accepted(수락 완료)  → 공급자 통지 + 취소보상(테넌트 귀책, 공급자 무귀책)
  └─ 긴급 신규 order (trigger_type: early_checkout, priority: urgent)
       new checkout_at = 실제 퇴실시각, 잔여시간으로 우선순위 동적 결정
```
- **긴급 프리미엄 부담 주체 정책 필요**(호스트 카드 기본, 게스트 귀책이면 위약금 연계 검토 — §13 #3).

### 백업 공급자 사전 지정 (v5 — Turno 벤치마킹)

Turno는 호스트가 **선호 청소부를 미리 지정**하고, 그가 거절하면 마켓플레이스로 넘긴다.
같은 매물을 같은 사람이 계속 치우는 편이 품질에 유리하다 — 구조를 익히기 때문이다.

```
매물 단위 백업 체인:  1순위 지정 공급자 → (거절/무응답) → Wave 1 → ... → Wave 4(직영)
```

- 사전 지정이 있으면 **Wave 0**처럼 우선 오퍼한다. 수락창은 짧게(T0).
- **강제 배정이 아니다** — 지정된 공급자도 거절할 수 있다(원칙 D).
- ⚠️ **원칙 C(공정성)와 충돌한다.** 지정이 늘면 신규 공급자가 콜을 못 받아 랭킹 공정성이 무의미해진다.
  허용 여부·비율 상한은 §13 신규 항목으로 열어둔다.

### 안전장치
- 매칭 기준: 활동지역 접두(필수)·가용 시간대·평점·신뢰도·(미래)매물 히스토리.
- 공정성 훅: 직영팀도 동일 규칙, 오퍼 이력 전량 기록.
- 정지(suspended)·차단(banned) 공급자는 **풀 산출 단계에서 제외**(§14 D).
- ⚠️ **`pg_net` fire-and-forget 리스크**(ARCASOS 교훈): 배차·결제·보상 같은 임계 플로우는 **재시도 + reconciliation 백스톱** 필수(§8).

---

## 7. 부트스트랩 전략

```
ARCASOS(수요 테넌트 #1) ──▶ 클린콜 ──▶ 창업자 직영 청소업(공급자 #1)
        └──────────── 루프 전체가 창업자 하나로 닫힘 (외부 참여자 0) ────────────┘
```
- **1일차부터 실매출·실단가 학습**: 청소 원가·소요시간·마진을 몸으로 알아야 수수료를 제대로 매김.
- **공급자 앱 dogfooding**: 창업자가 배차받는 쪽도 직접 사용.
- **확장 순서**: dogfood 검증 → 인접 롱테일 숙박 운영자에게 동일 API 개방(Tier 1 Hosted Link) → "다수 숙박 ↔ 다수 업체" 점화. 대형 OTA는 초기 고객 아님.

### 호스트 직접 경로 (v5 — 조사 결과, 착수는 나중)

테넌트 연동 없이 **호스트가 직접** 붙는 경로. Turno의 주 유입 경로이기도 하다.

| 항목 | 상태 |
|---|---|
| **에어비앤비 iCal** | ✅ **검증됨.** 예약 캘린더를 iCal로 내보내며 Turno가 이걸로 동작한다 |
| **국내 OTA**(야놀자·여기어때) | ❓ **미확인.** iCal 내보내기 제공 여부를 조사해야 한다 |
| **중단기 임대**(삼삼엠투 등) | ⚠️ **가치 낮음.** 주 단위 이상이라 턴오버 빈도가 낮다. 자동 감지의 이득이 작다 |
| **미지원 플랫폼** | 수동 등록으로 충분. 월 몇 건이면 사람이 넣는 게 빠르다 |

**착수 시점: 공급자 확보 이후.** 호스트를 모아도 치울 사람이 없으면 의미가 없다.
양면 시장에서 먼저 채워야 하는 쪽은 공급이다(원칙 C).

---

## 8. ARCASOS 연동 명세 + 발주 원장(SoR)

**핵심: 클린콜 = 발주 원장의 SoR(System of Record). 아르카 = webhook 미러.**

**연동 흐름**:
```
ARCASOS 이벤트 (체크인 → 발주 / 체크아웃 → 청소 시작 트리거)
  → POST /v1/orders {property, checkin_at, checkout_at, spec, trigger_type, tenant_ref=booking_id}
    → [클린콜 배차 엔진 + 발주 원장 기록]
      → webhook (billing_verified/dispatched/completed/charged/cancelled/failed + 귀책·금액)
        → ARCASOS가 자기 booking 옆에 발주 상태 요약 미러링
          → 호스트 문의·정산 대사·차후 보상에 활용
  ※ 청소값 결제는 아르카 미경유, 클린콜↔호스트 직거래(빌링키, §9)
```

**발주 원장·보상 추적 (지금 심어야 나중에 성립)**:
- **상호 참조 ID**: 클린콜 `order_id` ↔ 아르카 `booking_id`. 양쪽 저장 → 어느 쪽 문의든 단일 지목.
- **원본/미러 분리**: 원본(상태·이력·증빙·귀책·금액)은 클린콜. 아르카는 상태 요약 미러.
- **보상 집행도 SoR 기준**: 클린콜이 원장 근거로 판단.
- ⚠️ **reconciliation 필수**: webhook 유실(pg_net) 시 미러 어긋남 → 보상 대사 깨짐. 재시도 + 아르카가 주기적으로 order 상태 pull해 대조. **보상 데이터라 fire-and-forget 금지.**

**보상 유형별 필요 데이터**(정책은 나중, 필드 자리는 지금 — `claim` 테이블 골격):
| 보상 유형 | 필요 데이터 |
|---|---|
| 청소 하자 → 호스트/게스트 보상 | 완료증빙·귀책·청구액·접수시각 |
| 미배차/펑크 → 다음 예약 피해 | 실패시각·리드타임·대체조치 |
| 파손 배상 | 공급자 식별·보험 연계·사고기록 |
| 긴급배차 프리미엄 정산 | 프리미엄 금액·부담 주체 |

**claim → 공급자 정지 연계(v4)**: 귀책(fault)이 provider인 claim은 §14 D의 정지 신호에 가중된다. 정지 판단 근거는 항상 원장(SoR).

**매핑/옵트인**: 아르카 `property`·`host` ↔ 클린콜 매핑(신원 분리). 호스트가 "자동배차 사용" 선택 시 빌링키 등록돼야 발주 가능(§9), 미등록이면 발주 차단·안내.

---

## 9. 결제 / 세금 / 연결 구조 (확정)

### 결제 흐름
```
게스트 청소비 → 호스트에게 정산 (아르카, 종전과 동일 — 클린콜 무관)
호스트 청소값 → 호스트가 클린콜에 직접 결제 (빌링키)
클린콜 → 공급자 지급 (개인=3.3% 원천징수 / 업체=세금계산서 정산)
```

### 결제 타이밍 — 발주 시 검증 / 완료 후 청구
```
발주(체크인)   → 빌링키 유효성 검증 (₩0 auth 또는 등록상태 확인, 청구 X)
                  유효 → 배차 진행 / 무효·미등록 → 배차 보류 + 호스트 알림
청소 시작(체크아웃)
완료 보고     → 빌링키 재결제 (실제 청구) → 공급자 지급
취소·중도퇴실 → 청구 없음 (환불 로직 불필요)
```
- **근거**: (1) 완료 후 청구 = 전자상거래법상 "용역 제공 → 대가"로 깨끗(완료사진이 근거). (2) 취소 건은 애초 청구 없음 → 환불 코드 불필요. (3) 조기청구 분쟁으로 아르카 예약에 불똥 방지.
- **유일 리스크(완료 시 카드실패)를 발주 시 유효성 검증으로 선제 차단** → "일하고 못 받는" 실패율 급감.
- **표준 참고**: 우버·호텔의 auth(예약 시 승인) & capture(이용 후 청구). 단 국내 빌링키는 보통 즉시 재결제 방식이라 순수 2단계 pre-auth 지원 여부는 **PG(PortOne) 상담 확인**(§11).

### 결제 연결 — 3-tier (겉 UX 다양, 볼팅 코어 단일)
공통 코어: **카드/빌링키 = 클린콜·PG만 보유, 테넌트 = 참조 토큰만.** PG 결제창 방식(카드가 서버 미경유)으로 PCI 부담·유출 리스크 원천 제거.

| Tier | 방식 | 대상 | 개발부담 |
|---|---|---|---|
| **1** | **Hosted Link** (결제등록 URL/QR 전달) | 롱테일 OTA·개인 운영자·PMS | ≈ 0 |
| **2** | **OAuth 리다이렉트** ("클린콜로 연결") | 자체 앱/웹 있는 중급 테넌트(**아르카 레퍼런스**) | 중 |
| **3** | **Embed SDK** (테넌트 화면에 위젯) | 최대 편의 원하는 대형 테넌트 | 최대 |

- **호스트 편의**: 간편결제(카카오·네이버·토스) 우선 노출 → 카드 16자리 입력 없이 탭 몇 번.
- **자동 프로비저닝**: 호스트는 "클린콜 회원가입" 화면을 안 봄. tenant-scoped 계정 자동 생성.
- ⚠️ **동의는 호스트 본인이 직접**: 빌링키 등록·정기결제·개인정보 처리 동의는 결제창에서 호스트가 직접 체크(전자금융거래법·개인정보보호법 — 아르카가 대신 동의 불가). 이게 아르카 책임 최소화와도 직결.
- **다중 테넌트**: 같은 사람이 아르카·야놀자 양쪽에 붙어도 초기엔 테넌트별 별도 host 레코드·별도 빌링키(격리). 통합 아이덴티티는 나중(과설계 방지).
- **한 번 등록 → 영구 자동청구**(빌링키 재결제). 연결 해제 시 빌링키 삭제(revoke).

### 세금 / 고용
- **클린콜 → 호스트에게 세금계산서/현금영수증 발행**(공급-수급 일치). 발행 시스템은 클린콜 기능.
- 공급자 = **독립 계약자**. 개인(이모님) = 3.3% 원천징수 / 업체 = 사업자 정산·세금계산서. 클린콜 = 매칭·배차 대행(직접 고용/파견 회피).
- **원칙 D(강제 배정 금지)가 이 구조의 실무적 방패**다. 배차 UX가 "명령"으로 보이면 세무·노무 프레임이 무너진다.
- 아르카는 자금 통과 역할 아님 → **전자지급결제대행·자금보유 규제 회피**.
- 청소는 지원 서비스라 숙박업·부동산중개 라인과 무관. 제3자 서비스 면책 프레임 적용.
- ⚠️ 전자금융·근로·세무는 전문가 검토 권장.

---

## 10. 기술 스택 + 디자인 시스템 + 포팅할 검증 패턴

**스택(ARCASOS와 동일 계열, 독립 인스턴스)**: 프론트 = React + TS + Vite + Tailwind / 백엔드 = **독립 Supabase** + Deno EF.

### 디자인 (v4에서 전면 교체)

> **디자인: 클린콜 자체 시스템(라이트 + 파랑).** 아르카소스의 Linear Dark를 **재사용하지 않는다.**
> 토큰 SSOT = `src/index.css`의 oklch 시맨틱 변수(background / primary / accent / secondary / foreground × 11단계, `.dark` 변형 포함).
> primary(파랑) = 브랜드·액션·진행, accent(초록) = 성공·완료 전용, red/amber = 실패·경고.
> 원시 Tailwind 팔레트 및 하드코딩 hex 사용 금지. 상세 규격은 「디자인 시스템 전환 애드덤」 참조.

- 적용 범위: **클린콜 전체** — 랜딩 + 관리 콘솔 + 공급자 앱.
- ⚠️ **`design-agent` 스킬은 아르카소스 전용.** 클린콜 레포에 적용하지 않는다. 두 디자인 시스템을 섞지 말 것.
- 폰트 `Noto Sans KR` 단일, `font-bold` 금지(`font-semibold`까지). 아이콘 remixicon(`ri-*`).
- 금지 패턴 grep 목록(원시 팔레트 / 하드코딩 hex / `transition-all` / `font-bold` / Linear Dark 잔재)은 CI 또는 PR 체크리스트에 편입(§11).
- 상태 배지 색 매핑은 발주 생명주기(§4)·공급자 상태(§14)와 1:1 대응 — 애드덤 §C 표가 유일 해석.
- 다크모드: 토큰은 준비됨, 켤 시점 미정(§13 #9).

**ARCASOS에서 포팅할 검증 패턴(재구축 금지, 이식)**:
- 알림톡/SMS: Aligo + Gabia proxy(고정 IP `211.47.74.55` — Gabia NS 유지). 단일 EF(SSOT) 다채널 발송.
- 결제: 빌링키(PortOne/Danal). 자금보유 없는 직청구 모델.
- 3.3% 원천징수 로직.
- MCP / A2A 인프라(배차 오케스트레이션).
- SEC-04(service_role EF caller 검증), Supabase Data API 4단계 SQL(§11), Realtime flicker 방지, ErrorBoundary.
- ⚠️ **디자인 시스템은 포팅 대상이 아니다**(위 참조).

---

## 11. 셋업 체크리스트 (구현 착수 시)

**인프라**
- [ ] 도메인 확정(§13) — Gabia 유지 권장(알림톡 고정 IP)
- [ ] 신규 GitHub 레포 / 신규 Supabase 프로젝트(prod/dev ref 분리)
- [x] DB 스키마 (21개 테이블): `tenants`, `hosts`, `providers`, `provider_service_areas`, `provider_events`, `orders`(원장 필드+상호참조), `order_events`(append-only), `order_offers`(오퍼 이력), `properties`, `billing_keys`, `payments`, `payouts`, `claims`(골격), `region_codes`, **`property_access_info`, `order_access_info`, `access_update_tokens`, `access_info_views`, `webhook_deliveries`, `order_issues`**
- [x] **Supabase Data API 4단계 SQL**(테이블마다): CREATE → GRANT → ENABLE RLS → CREATE POLICY (누락 시 PostgREST `42501`)
- [x] `region_codes` 서울 493행 적재 (시도 1 + 자치구 25 + 법정동 467)
- [x] **env 분리** — `env` 컬럼(DEFAULT 없음) + 복합 FK로 교차 참조 차단

**API·엔진**
- [x] API 키 발급/검증 (`_shared/auth.ts` — `ck_(live|test)_<48hex>`, sha256 저장)
- [x] `POST /v1/properties` · `GET /v1/properties/{id}` (지역 해석·호스트 자동 프로비저닝)
- [x] `POST /v1/orders` (멱등 2겹·배차 게이트·`deadline_at` 계산. **배차는 하지 않음**)
- [x] `POST /v1/sandbox/orders/{id}/transition` (테스트 키 전용 강제 전이)
- [x] **webhook 전송** — 트리거 enqueue + 워커 EF + cron 백스톱 + 하트비트 + 시각 기준 만료
- [ ] 나머지 조회·변경 API (`GET /orders`, `PATCH`, `cancel`, `messages`, `claims`, `coverage`)
- [ ] **reconciliation** — 테넌트가 주기적으로 order 상태를 pull해 대조하는 경로(§8)
- [ ] 배차 엔진 EF(**오퍼 웨이브 1~4**/에스컬레이션/백업/중도퇴실 긴급) + 오퍼 이력 기록
- [ ] 공급자 오퍼 수락/거절 EF (강제 배정 경로 없음 확인 — 원칙 D)

**공급자 온보딩(v4 신규)**
- [ ] 국세청 **사업자등록정보 진위확인** + **사업자등록 상태조회** API 연동(공공데이터포털). 키 발급·엔드포인트 재확인, **일일 호출 한도 → 결과 캐시**
- [ ] 본인인증(PASS/휴대폰) + 계좌 실명확인 연동
- [ ] **법정동코드 시드 데이터 적재**(행정표준코드관리시스템) + `properties.region_code` 채번
- [ ] `provider_events` append-only 기록 경로(온보딩·경고·정지·해제 전부)
- [ ] 공급자 정지/해제 운영자 화면 + 사유 필수 입력 게이트

**결제·세무**
- [ ] 결제 연결 EF 3-tier(Hosted Link/OAuth/Embed) + 자동 프로비저닝 + 동의 수집
- [ ] 빌링키: **발주 시 유효성 검증** + **완료 후 재결제** + 실패 재시도·배차정지
- [ ] **PG(PortOne) 상담 확인 항목**: 간편결제(카카오/네이버/토스) 빌링키 지원 범위 / pre-auth(auth-capture) 지원 여부 / 정기결제 입점 심사
- [ ] 세금계산서·현금영수증 발행 EF(클린콜 → 호스트)
- [ ] 공급자 지급 EF(개인 3.3% 원천징수 / 업체 사업자 정산)

**프론트·디자인**
- [ ] `src/index.css` 토큰 블록에 **"SSOT — 임의 수정 금지"** 주석 삽입
- [ ] 금지 패턴 grep 목록을 CI 또는 PR 체크리스트에 편입(§10)
- [ ] 클린콜 레포에 `design-agent` 스킬 미적용 명시
- [ ] 공급자 앱(콜 수신/수락/완료사진)
- [ ] 랜딩 v1 잔여 수정(애드덤 §H): Navbar 앵커 `id` 누락 / 외부 이미지 11장 자체 호스팅 / `ErrorBoundary` 추가 / 운영자 카드·Hero 카피 정정 / `index.html` canonical·og:url 도메인 확정 전 처리 / i18n 배선 정리 / `NotFound.tsx` 재작성

**연동·법무**
- [ ] ARCASOS ↔ 클린콜 property·host 매핑 + 체크인/체크아웃/중도퇴실 이벤트 연동
- [ ] 법적/세무 구조 문서화(§9) → 계약·약관 템플릿
- [ ] **약관에 배차 일시중지/해지 근거 명시**(§14 D — 없으면 부당 배차중단 분쟁)

---

## 12. 범위 밖 (v4+ — 지금 하지 않음)

- 입주청소·정기청소·소비자 대상 청소(STR 턴오버만)
- 실시간 초단위 매칭 엔진(예약형 오퍼 웨이브로 충분)
- 대형 OTA 직접 연동(초기 고객 아님)
- 청소용품/베딩/수하물 등 인접 서비스 번들
- 복잡한 품질 스코어링/AI 매칭(초기엔 지역·평점·신뢰도 룰)
- 통합 호스트 아이덴티티(초기엔 테넌트별 격리)
- 아르카 경유 청소비 대납/정산 차감(폐기 — 호스트 직결제로 대체)
- **사업자등록증 이미지 자동 OCR**(진위 근거는 API, 이미지는 육안 대조·보관용)
- **반경(km) 기반 지오 매칭**(지금은 행정구역 접두만)
- **다크모드 실제 적용**(토큰만 준비, §13 #9)
- **서울 외 지역 확장**(법정동코드 구조상 가능하나 초기 시드는 서울)

---

## 13. 오픈 결정 사항 (남은 것)

1. **도메인** — 미정 (예: cleancall.co.kr 등).
2. ~~콜 분배 초기 정책~~ → ✅ **v4 확정: 랭크드 오퍼 웨이브(하이브리드)**, MVP는 Wave 1 생략 가능(§6).
3. ~~긴급 프리미엄 부담 주체~~ → ✅ **v5 확정: 현재 0원.** 중도퇴실 빈도·원가 데이터가 없는 상태에서
   근거 없는 금액을 부과하지 않는다. 정책 도입 시 **최소 60일 전 통지**하며 발생 조건과 상한을 함께 명시한다.
   그 전까지 `amounts.urgent_premium`은 항상 0.
4. ~~완료 자동확정 시간(N)~~ → ✅ **v5 확정:**
   ```
   auto_confirm_at = max( completed_at + 6h, deadline_at )
   ```
   - **하한 6시간** = 호스트가 완료 알림을 받고 확인할 최소 시간.
   - **`deadline_at` 이전에는 확정되지 않는다** = 불량이 발견될 수 있는 최초 시점 전에 확정하면 검증 기회가 사라진다.
   - **클레임은 `completed` 후 7일.** `confirmed`·`charged` 이후에도 접수 가능하다 — 확정은 청구를
     진행시키는 절차일 뿐이고 보상은 별도 트랙이다. 다음 게스트가 입주한 뒤 불량이 발견되는 경우를 고려한다.
   - **공급자 지급은 클레임 기한(7일) 경과 후.** 기한 내 클레임이 접수되면 해당 건 지급을 보류한다.

   > ⚠️ **왜 상한(`least(..., next_checkin_at - 1h)`)이 없는가** — 나중에 반드시 다시 질문될 지점이다.
   >
   > 상한이 있던 구버전은 **당일 턴오버 전제**였다. 체크아웃 당일 다음 게스트가 들어오니
   > "다음 입실 1시간 전에는 무조건 확정"이 안전판 역할을 했다.
   >
   > 아르카 회신에서 실제 운영이 **주 단위 임대**로 드러나면서 그 전제가 깨졌다. 리드타임이 1주 이상이면
   > `next_checkin_at - 1h`는 훨씬 뒤라 상한으로 기능하지 않는다.
   >
   > 동시에 클레임을 `confirmed`·`charged` 이후에도 7일간 열어두기로 하면서 **상한이 하던 일을
   > 클레임 트랙이 대신하게 됐다.** 확정이 늦어도 보상 기회가 닫히지 않으므로 서두를 이유가 없다.
   >
   > 당일 턴오버가 늘면 재도입을 검토한다(§15). **다만 지금 미리 넣지 마라** — 주 단위에서는
   > 발동하지 않는 분기라 검증이 안 되고, 검증 안 된 코드는 나중에 조용히 틀리게 동작한다.
5. **보상(claim) 정책 상세** — 유형별 지급 기준·한도·집행 절차(골격은 §8, 정책 미정).
6. **공급자 앱 형태** — 웹앱 vs 네이티브(초기엔 웹앱 권장).
7. **직영 청소업 사업자 구조** — 클린콜 법인과 분리 여부.
8. **PG 상담 결과 반영** — 간편결제 빌링키·pre-auth 지원 확인 후 결제 타이밍 세부 확정.
9. **다크모드 적용 시점** (v4 신규) — 토큰은 준비됨, 켤지·언제 켤지 미정(§10).
10. ~~공급자 자동승인 여부~~ → ✅ **v5 확정: 운영자 최종 승인.**
    진위 valid + 계속사업자여도 자동 `active`로 보내지 않고 **`pending_review`를 경유**한다.
    자동 승인은 되돌리기 비싸다 — 부적격 공급자가 실제 매물에 들어간 뒤에는 신뢰 회복이 안 된다.
11. **자동 정지 임계값** (v4 신규) — 평점 하한·노쇼 횟수·claim 발생률의 구체 수치(§14 D).
12. **Wave 파라미터** (v4 신규) — T1 수락창 길이, Wave 1 대상 인원수, 인센티브 상향 폭(§6).
13. **무보상 변경·취소 시한 24시간** (v5 신규) — **제안값이다.** `free_change_until = scheduled_at - 24h`로
    구현돼 있으나 확정이 아니다. **공급자 약관과 함께 정해야 한다** — 공급자가 언제까지 일정을 비워두는
    대가를 받는지와 짝이기 때문이다. 아르카가 **연속거주 연기 빈도 데이터**를 회신 예정.
14. **단가 체계** (v5 신규) — 현재는 매물 단위 `base_price` 고정. 원가 데이터 축적 후 재설계한다.
    **입찰 방식 도입 여부**도 여기서 함께 본다(§1 — Turno는 청소부 입찰).
15. **`webhook_max_age` 48시간** (v5 신규) — 이 시간을 넘긴 이벤트는 발송하지 않고 `dead` 처리한다.
    백오프 6회 소진(약 31시간)보다 길어야 정상 재시도를 죽이지 않아 48로 잡았으나, **운영 데이터 없이
    정한 임계값이다.** 프로젝트 일시정지 후 재개 시 밀린 것이 한꺼번에 나가는 것을 막는 장치다.
16. **`preferred_provider` 허용 여부** (v5 신규) — 매물별 선호 공급자 사전 지정(§6 백업 체인).
    **원칙 C(공정성)와 정면으로 충돌한다.** 허용할지, 허용하면 비율 상한을 둘지 결정 필요.

> ✅ v3에서 해소됨: 결제 연결 방식(3-tier + 자동 프로비저닝 + 동의 주체), 결제 타이밍(발주 검증/완료 청구), 발주 원장·보상 추적(SoR-미러 + 상호참조 ID + reconciliation).
> ✅ v4에서 해소됨: 콜 분배 정책(#2), 공급자 유형·온보딩 검증 흐름, 활동지역 모델, 배차 정지 상태머신, 디자인 시스템.
> ✅ v5에서 해소됨: 긴급 프리미엄(#3), 완료 자동확정 공식(#4), 공급자 승인 방식(#10).

---

## 14. 공급자 온보딩 · 활동지역 · 배차 정지 (v4 신설)

> 섹션 번호 안정성을 위해 §1~§13 뒤에 append. 내용상으로는 §4(객체)·§6(배차)의 확장이다.

### A. 공급자 유형 & 온보딩

| 항목 | 업체(`business`) | 개인(`individual`, 이모님) |
|---|---|---|
| 식별 | 사업자등록번호 | 본인인증(휴대폰) |
| 검증 | 국세청 진위확인 + 상태조회 | 본인인증 + 계좌 실명확인 |
| 지급 | 세금계산서/사업자 정산 | **3.3% 원천징수** |
| 필수 입력 | 상호·대표자·개업일·등록증 이미지·지급계좌·활동지역 | 성명·연락처·지급계좌·활동지역 |

**사업자등록증 검증 흐름**
```
업체 입력: 사업자등록번호 + 대표자 성명 + 개업일자 (+ 등록증 이미지)
  → 국세청 "사업자등록정보 진위확인" API (공공데이터포털)
       번호+대표자+개업일 일치 여부 → valid / invalid
  → 국세청 "사업자등록 상태조회" API
       계속사업자 / 휴업 / 폐업
  → (진위 valid AND 계속사업자) → 자동 승인 후보 → 운영자 최종 승인 or 자동 active (§13 #10)
  → (invalid / 휴폐업) → 반려 + 사유 안내
```
- ⚠️ **엔드포인트·파라미터·키 발급 절차는 구현 시점에 공공데이터포털에서 재확인**(스펙 변동 가능). 진위확인 API는 일일 호출 한도가 있으므로 **결과 캐시**.
- 등록증 이미지는 육안 대조·보관용. 진위의 근거는 어디까지나 API(자동 OCR은 범위 밖, §12).

**온보딩 상태**
```
registered (가입) → pending_review (검증 대기) → active (배차 대상)
                                              └ rejected (반려, 사유 표기)
```

**DB 델타**
- `providers`: `id`, `type(business|individual)`, `status`(온보딩 + 정지 상태머신 공유), `display_name`, `phone`, `bank_account`, `rating`, `created_at`
- 업체 전용: `business_no`, `rep_name`, `open_date`, `biz_verify_status(valid|invalid|pending)`, `biz_status(active|closed|suspended_tax)`, `license_image_url`
- `provider_service_areas`: `provider_id`, `region_code`, `level(city|gu|dong)` — B 참조
- `provider_events`(append-only): `provider_id`, `event(registered|verified|rejected|activated|warned|suspended|reinstated|banned)`, `reason`, `actor`, `at` — 온보딩·정지 이력 공용 원장

### B. 활동지역 모델 (법정동코드 접두 계층)

공급자가 **서울 전역 / 특정 구 / 특정 동**을 자유 조합으로 고른다. 저장·매칭은 **법정동코드 접두(prefix)**.

```
법정동코드 계층 (행정표준코드관리시스템 기준)
  서울특별시 = "11"          (2자리 접두)
  강남구     = "11680"       (5자리 접두)
  역삼동     = "1168010100"  (전체 코드)

공급자 선택 저장 = { region_code, level } 행들
  "서울 전역" → { "11", city }
  "강남구"    → { "11680", gu }
  "역삼동"    → { "1168010100", dong }
```

**매칭 규칙**(§6 풀 산출에서):
```
property.region_code(10자리)가 공급자 선택 접두 중 하나로 시작하면 → 커버리지 있음
  매물 = 1168010100(역삼동)
    공급자 A "11"(서울전역)  → startsWith → 매칭 O
    공급자 B "11680"(강남구) → startsWith → 매칭 O
    공급자 C "11710"(송파구) → 불일치     → 매칭 X
```
- 장점: 계층 확장/축소가 접두 길이로 자연 표현, 매칭이 문자열 접두 검사 한 줄.
- ⚠️ 실제 코드값은 행정표준코드관리시스템에서 가져와 시드 데이터로 적재(위 예시는 형식 설명용).

### C. 배차 정지 (공급자 상태머신)

**"청소 결과에 큰 불만이 누적된 공급자를 배차 풀에서 제외"** 하는 안전장치.

```
active ─(경고 사유)→ warned ─(임계 초과/중대 사유)→ suspended ─(재심)→ reinstated(→active)
   └────────────────(중대·반복)───────────────────────────────→ banned (영구)

정지(suspended) 효과:
  - 배차 풀 산출에서 제외 → 신규 콜 안 뜸
  - 진행 중(accepted/in_progress) 발주는 그대로 마무리 (중도 회수 시 재배차 비용 발생)
  - 정지 기간 / 무기한 선택 가능
```

**트리거**
- **수동**: 운영자가 claim·불만 검토 후 정지 (사유 필수 입력).
- **자동(임계)**: 평점 하락(예 4.0↓) / 노쇼 N회 / claim 발생률 / 완료증빙 미제출률 → 자동으로 `warned` 또는 `suspended` **후보**로 올려 운영자 확인. 구체 수치는 §13 #11.

**공정성·법적 요건**
- 정지·해제 모두 `provider_events`에 **append-only 로그**(사유·행위자·시각). 임의 차단이 아님을 감사 가능하게.
- 정지 사유 유형은 **약관에 근거 명시**(독립계약자 계약상 일시중지/해지 근거). 없으면 부당 배차중단 분쟁.
- **완전 자동 영구밴 금지** — 자동 판정은 사람 확인 게이트를 거친다(오탐 방지).
- claim 연계: 귀책이 provider인 claim은 정지 신호에 가중. 판단 근거는 원장(SoR, §8).

### D. 관련 화면
공급자 가입(유형 선택 / 업체 등록 / 개인 등록 / 활동지역 선택)과 콘솔 공급자 관리·정지 모달의 상세 UI 스펙 및 Readdy 프롬프트는 「디자인 시스템 전환 애드덤」 §G(G1~G5)에 있다. **§10의 토큰 규칙을 벗어나는 프롬프트는 사용 금지**(구 Linear Dark 프롬프트는 폐기됨).

---

## 15. 숙박 기간 확장 시 재검토 (v5 신설)

> §14와 같은 이유로 뒤에 append한다 — §12는 이미 "범위 밖"이고, 이 문서는 섹션 번호 안정성을
> 명시적 규칙으로 둔다. 번호를 밀면 애드덤·외부 참조가 깨진다.

**현재 설계는 주 단위 임대(리드타임 1주 이상) 전제다.** 아르카 회신에서 실제 운영이 그렇게 드러났고,
그 전제 위에서 여러 값이 정해졌다. 2~3일 숙박으로, 나아가 당일 턴오버로 확장하면 아래가 먼저 깨진다.

| 항목 | 현재 | 짧아지면 |
|---|---|---|
| **자동확정 상한** | 없음. `max(completed_at + 6h, deadline_at)` | 당일 턴오버가 늘면 `least(..., next_checkin_at - 1h)` 재도입 검토. 지금은 클레임 트랙(completed 후 7일)이 그 역할을 대신한다 |
| **`cleaning_deadline_hours`** | 기본 24h | 짧은 숙박에서는 과도하다. 매물 단위로 조정 가능하지만 기본값 자체를 다시 봐야 한다 |
| **무보상 시한** | `scheduled_at - 24h` | 마감 변경이 임박해서 들어오는 빈도가 올라간다. 24h가 실질적으로 "항상 보상 대상"이 될 수 있다 |
| **재배차 경로** | 리드타임 여유로 흡수 | 주 단위에서는 거의 발동하지 않아 **실전 검증이 부족하다.** 짧아지면 갑자기 핵심 경로가 된다 |

> ⚠️ **자동확정 상한을 지금 미리 넣지 마라.** 주 단위에서는 발동하지 않는 분기라 검증이 안 되고,
> 검증 안 된 코드는 나중에 조용히 틀리게 동작한다. 필요해지는 시점에 그때의 데이터로 넣는다.

**구조는 이미 열려 있다.** 시각 필드가 전부 있고(§4) 임계값이 매물 단위로 조정 가능하므로,
확장 시 바꿀 것은 **값이지 구조가 아니다.** 스키마 마이그레이션 없이 대응 가능하다는 뜻이다.

---

*이 문서는 클린콜 프로젝트의 SSOT입니다. 결정이 추가/변경되면 반드시 이 문서에 반영하세요.
**v4→v5에서 이 규칙을 어겨 SSOT가 레포에서 가장 오래된 문서가 된 적이 있습니다. 반복하지 마세요.** 「공급자 온보딩·배차 정책 애드덤」과 「디자인 시스템 전환 애드덤」은 v4에 흡수되었으며, 디자인 상세 규격(토큰값·컴포넌트·프롬프트)만 후자를 계속 참조합니다.*
